import { handlers } from "@/auth" 
export const { GET, POST } = handlers


//IMPORTENT>>>
// Iska use Auth.js ke authentication routes handle karne ke liye karte hain.
// Ye login, logout, session aur OAuth requests ko automatically manage karta hai backend side par.